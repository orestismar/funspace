This jupyter config file should be placed in C:\users\%USER_NAME%\.jupyter.

The most important element is to add the pre_save_hook which scrubs the notebook - Don't want to save data into notebooks (version control issues)